# produits/modeles — v2

CRUD modèles produits (§5.1 domain.md). PK `id_modele` **singulier** (contrat §2.1).

Endpoints :
- `GET /api/v2/produits/modeles`
- `GET /api/v2/produits/modeles/:id`
- `GET /api/v2/produits/modeles/:id/variantes` — articles catalogue liés
- `POST /api/v2/produits/modeles`
- `PUT /api/v2/produits/modeles/:id`
- `DELETE /api/v2/produits/modeles/:id`
