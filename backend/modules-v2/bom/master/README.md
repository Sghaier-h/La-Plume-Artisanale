# bom/master — v2

CRUD BOM Master — en-tête nomenclature par article (§7.2 domain.md).
Contrainte : une seule BOM active (`est_active=true`) par article (unique partiel DB).
Endpoints CRUD standard sous `/api/v2/bom/master`.
