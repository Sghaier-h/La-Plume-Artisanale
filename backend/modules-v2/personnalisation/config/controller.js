// controller.js — personnalisation/config
// Contrôleur CRUD standard. Ré-exporte la version générée par crudFactory.
import { baseController } from './model.js';

export const controller = {
  ...baseController,
};

export default controller;
