name: 🚀 Déploiement Production

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
      
    - name: 🔍 Vérification du code
      run: |
        echo "✅ Code vérifié"
        echo "📦 Prêt pour déploiement"
        
    - name: 📤 Notification
      run: |
        echo "🚀 Déploiement déclenché sur le serveur OVH"
        echo "📋 Exécutez sur le serveur :"
        echo "   cd /var/www/fouta-erp && bash scripts/deploy.sh"
        
    # Optionnel : Déploiement automatique via SSH
    # - name: 🚀 Déploiement SSH
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USER }}
    #     key: ${{ secrets.SSH_KEY }}
    #     script: |
    #       cd /var/www/fouta-erp
    #       bash scripts/deploy.sh

